# КонтекстПревышенияЛимита

## Синтаксис:

```bsl
Новый КонтекстПревышенияЛимита(<ЛимитВызовов>, <ИнтервалЛимита>, <КоличествоВызововЗаИнтервал>, <ОсталосьОжидать>)
```

**Параметры:**

| Имя | Тип | Описание |
| -- | -- | -- |
| ЛимитВызовов | Число | Максимально допустимое количество вызовов за интервал |
| ИнтервалЛимита | Число | Длительность интервала лимита в миллисекундах |
| КоличествоВызововЗаИнтервал | Число | Количество уже выполненных вызовов за интервал |
| ОсталосьОжидать | Число | Сколько миллисекунд осталось до следующего доступного вызова |


## Методы

[ЛимитВызовов](#лимитвызовов) </br>
[ИнтервалЛимита](#интерваллимита) </br>
[КоличествоВызововЗаИнтервал](#количествовызововзаинтервал) </br>
[ОсталосьОжидать](#осталосьожидать)


## ЛимитВызовов

**Синтаксис:**

```bsl
ЛимитВызовов()
```

**Возвращаемое значение:**

Тип: Число.

**Описание:**

Возвращает лимит вызовов за интервал.


## ИнтервалЛимита

**Синтаксис:**

```bsl
ИнтервалЛимита()
```

**Возвращаемое значение:**

Тип: Число.

**Описание:**

Возвращает длительность интервала лимита в миллисекундах.


## КоличествоВызововЗаИнтервал

**Синтаксис:**

```bsl
КоличествоВызововЗаИнтервал()
```

**Возвращаемое значение:**

Тип: Число.

**Описание:**

Возвращает количество уже выполненных вызовов в текущем интервале.


## ОсталосьОжидать

**Синтаксис:**

```bsl
ОсталосьОжидать()
```

**Возвращаемое значение:**

Тип: Число.

**Описание:**

Возвращает время ожидания до следующего разрешенного вызова в миллисекундах.
